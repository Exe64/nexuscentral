node_modules/
dist/
build/
coverage/
*.tsbuildinfo

.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Local database volumes
.data/

# Database dumps. deploy.sh writes here on the VPS; they hold every item, source
# and stored credential, so they must never reach the repository.
backups/
control/
